apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: gateways-issuer
spec:
  selfSigned:
    $patch: delete
  acme:
    email: rcarey@mitre.org
    server: https://acme-staging-v02.api.letsencrypt.org/directory
    privateKeySecretRef:
      name: gateways-issuer-account-key
    solvers:
    - dns01:
        route53:
          hostedZoneID: Z05295341EQEK44XS779A
          region: us-east-1
          # role: arn:aws:iam::177337816322:role/aim-city_cert-manager_cert-manager
